==テスト完了==
exportTest
	キーと値のObjectをjson形式の配列に直してファイルで出力するやつ

==今後の予定==
displayTest
	ギャルゲでよくある１文字ずつ表示するやつ
yesOrNo
	透過しないボーダーでyesOrNoを取る感じ(単純に押したら返り値にtrueOrFalse返せばいい、後は呼ぶ側の問題)
	どちらかというとcss作るの頑張ってというところが強い(他のボタンの判定消さなきゃいけないし
randomTest
	レッスンやオーディションで使う予定の乱数生成テスト
	範囲固定(例えば0~1000)で別テーブルから参照させるのがいい気がする
	引数に範囲上限指定させて、あとは返り値ということにしよう
communicationTest
	選択肢を表示して分岐させるやつ

==次回実装関係のメモ==
